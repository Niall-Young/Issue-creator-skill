interface:
  display_name: "GitHub Issue Handoff"
  short_description: "把项目任务交接成 Agent 可直接执行的 GitHub Issue"
  default_prompt: "Use $github-issue-handoff to turn this GitHub link and my task description into an agent-ready Issue and submit it."

policy:
  allow_implicit_invocation: true
